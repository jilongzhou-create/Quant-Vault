#!/usr/bin/env python3
"""
LLM Prompt - SPY Satellite Factor Mining V2 (Wall Street Physics Edition)

V2 核心改进 (基于首轮挖掘失败验尸):
  1. 强制脉冲信号: Trigger Rate 5%-15%, 常态必须返回 0.0
  2. 二阶导数铁律: 禁止"VIX>40则买入", 必须"极值+衰竭"才抄底
  3. 边际变化铁律: FOMC等低频数据必须用.diff(), 禁止直接输出绝对值
"""

DIRECTION_DESCRIPTIONS = {
    'policy_pivot': (
        "方向1: 政策转向与流动性冲量\n"
        "目标: 捕捉美联储政策预期发生剧变的极短窗口(通常只有几天到两周)\n"
        "\n"
        "核心物理法则:\n"
        "  - 不要关注收益率曲线是否倒挂(绝对水位), 而要关注它的'动量变化'\n"
        "  - 真正的买点出现在'短端利率(2年期)剧烈下行导致曲线突然变陡'(Bull Steepening)的时刻\n"
        "  - 对于fomc_sentiment, 只捕捉它发生jump(跳跃/反转)的前几个交易日, 不是它的绝对值\n"
        "\n"
        "正确示例:\n"
        "  - T10Y2Y连续5天急剧上升(变陡) + 处于降息周期初期 → 强看多(+1.0)\n"
        "  - fomc_sentiment.diff()从负值突然跳升到+0.3以上(鸽派突变) → 看多(+1.0)\n"
        "  - DGS2在5天内下跌超过20个基点(市场抢跑降息) → 看多(+1.0)\n"
        "\n"
        "错误示例(绝对禁止):\n"
        "  - 'fomc_sentiment > 0 → 看多' (这是绝对值逻辑, 每天都触发, 垃圾!)\n"
        "  - 'T10Y2Y < 0 → 看空' (倒挂是常态, 不是信号)\n"
        "\n"
        "关键: 信号必须是脉冲型的! 只在预期发生剧变的瞬间触发, 其他时间必须返回0.0"
    ),
    'panic_mean_reversion': (
        "方向2: 恐慌极值与均值回归 (买入恐慌的'衰竭时刻')\n"
        "目标: 捕捉恐慌见顶回落的极短窗口, 这是美股独有的'抄底'物理属性\n"
        "\n"
        "核心物理法则 (二阶导数铁律):\n"
        "  - 极度恐慌产生的是买点, 但必须等待恐慌'见顶回落'!\n"
        "  - 绝对禁止'VIX > 40则买入'这种接飞刀逻辑! 高VIX期间直接买入会死于主跌浪\n"
        "  - 正确逻辑: '极值 + 衰竭' = VIX处于历史高位, 且今日VIX.diff() < 0(恐慌开始回落)\n"
        "  - 轻度恐慌(VIX缓慢上升)是钝刀割肉, 应输出看空(-1.0)或0.0\n"
        "\n"
        "正确示例:\n"
        "  - 计算VIX的252日Z-Score, 当Z-Score > 2.5(极端恐慌), 且VIX当天低于过去3天均值(恐慌衰竭)\n"
        "    → 输出强看多(+1.0)! 如果VIX还在创新高, 绝对保持0.0甚至-1.0\n"
        "  - BAMLH0A0HYM2的Z-Score > 2.0(信用利差极端), 且3日变化率由正转负(利差开始收窄)\n"
        "    → 输出强看多(+1.0)!\n"
        "  - VIX在5天内从25飙升到45(恐慌爆发), 然后第6天VIX下跌(恐慌衰竭)\n"
        "    → 输出强看多(+1.0)!\n"
        "\n"
        "错误示例(绝对禁止):\n"
        "  - 'VIX > 40 → 买入' (接飞刀! VIX可以到80, 你在40就买会死)\n"
        "  - 'BAMLH0A0HYM2 > X → 买入' (利差可以继续走阔, 必须等衰竭信号)\n"
        "  - 'VIX升高 → 看空' (这是连续信号, 不是脉冲, 且方向可能搞反)\n"
        "\n"
        "关键: 信号必须是脉冲型的! 只在恐慌极值+衰竭的瞬间触发, 其他时间必须返回0.0"
    ),
}

METHOD_DESCRIPTIONS = {
    'unstructured': (
        "方法A: 非结构化数据转化 (NLP Sentiment)\n"
        "目标: 将FOMC声明情绪得分的变化转化为脉冲信号\n"
        "\n"
        "可用数据(已入库):\n"
        "  - fomc_sentiment: FOMC声明鹰鸽情绪得分, 范围[-1.0, 1.0]\n"
        "    1.0=极度鸽派(看多美股), -1.0=极度鹰派(看空美股)\n"
        "    基于LLM对FOMC声明的文本分析, T+1生效, 非会议日前向填充\n"
        "    覆盖2007-02至今, 日频, 每年约8次FOMC会议\n"
        "\n"
        "边际变化铁律:\n"
        "  - 绝对禁止直接输出fomc_sentiment的绝对值! 它是低频阶梯状数据\n"
        "  - 必须使用.diff()或计算其动量变化\n"
        "  - 只有在预期发生改变的瞬间(跳跃/反转), 才触发信号\n"
        "\n"
        "正确示例:\n"
        "  - fomc_sentiment.diff() > 0.3 (鸽派突变) → +1.0脉冲\n"
        "  - fomc_sentiment.diff() < -0.3 (鹰派突变) → -1.0脉冲\n"
        "  - fomc_sentiment从负转正(鸽派转向) → +1.0脉冲(仅转向当天)\n"
        "\n"
        "错误示例(绝对禁止):\n"
        "  - 'fomc_sentiment > 0 → 1.0' (每天都触发, 垃圾!)\n"
        "  - 'fomc_sentiment * 0.5' (连续信号, 不是脉冲!)"
    ),
    'nonlinear': (
        "方法B: 非线性特征交叉\n"
        "将多个单维度数据的极值状态交叉成高维脉冲触发因子\n"
        "\n"
        "核心原则:\n"
        "  - 交叉条件必须基于经济学逻辑, 禁止无意义的排列组合\n"
        "  - 必须使用Z-Score或.diff()来识别'极值+转折'状态\n"
        "  - 信号必须是脉冲型: 只在条件满足的瞬间触发, 其他时间返回0.0\n"
        "\n"
        "正确示例:\n"
        "  - VIX_ZScore > 2.5 AND VIX_3day_diff < 0 (恐慌极值+衰竭) → +1.0\n"
        "  - T10Y2Y_5day_change > 0.1 AND DGS2_5day_change < -0.15 (曲线急剧变陡) → +1.0\n"
        "  - BAMLH0A0HYM2_ZScore > 2.0 AND BAMLH0A0HYM2_3day_diff < 0 (信用利差极端+收窄) → +1.0\n"
        "\n"
        "错误示例(绝对禁止):\n"
        "  - 'VIX > 30 AND T10Y2Y < 0' (绝对值交叉, 持续触发, 垃圾!)\n"
        "  - 'fomc_sentiment > 0 AND VIX > 20' (太平凡, 几乎每天都满足)"
    ),
}

SYSTEM_PROMPT_TEMPLATE = """你是一个顶级量化因子研究员, 专门为SPY(标普500 ETF)宏观趋势策略挖掘卫星因子。

## 绝对铁律 (违反任何一条, 因子将被直接永久废弃!)

1. 因子必须实现 BaseFactor 接口, 包含 calculate_signal(data: pd.DataFrame) -> pd.Series 方法
2. 信号输出范围必须是 [-1.0, 1.0], 正值看多美股, 负值看空美股
3. 禁止在因子内部引用以下 CoreAnchor 数据: indpro, icsa, walcl, wtregen, rrpontsyd
4. 因子逻辑必须纯粹, 只看自己领域的数据, 不做跨域交叉过滤
5. 所有阈值和参数必须有经济学含义, 禁止无意义的魔法数字

6. 【零值休眠铁律 - 最重要的规则!】:
   卫星因子必须是"狙击手"级别的脉冲信号(Pulse Signal)!
   常态下, 信号必须返回 0.0。
   只在极端事件发生的当天及随后极短几天内输出非零值(-1.0 或 +1.0)。
   目标 Trigger Rate 必须控制在 5% 到 15% 之间!
   如果你的代码每天都输出非零值, 将被直接永久废弃!

7. 【二阶导数铁律 (防接飞刀)!】:
   绝对禁止写出"如果 VIX > 40 则买入"这种愚蠢逻辑!
   高VIX期间直接买入会死于主跌浪。
   正确的抄底逻辑必须是"极值 + 衰竭":
   - VIX处于历史高位(Z-Score > 2.5)
   - 且今日VIX.diff() < 0(恐慌开始回落)
   - 此时才输出 +1.0

8. 【边际变化铁律!】:
   对于FOMC等低频阶梯状数据, 绝对禁止直接输出它的绝对值!
   必须使用 .diff() 或计算其动量变化。
   只有在预期发生改变的瞬间, 才触发信号。

9. SPY核心物理属性: 这是一个长牛+均值回归的市场
   - 极端恐慌信号应输出正值(抄底买入), 但必须等恐慌衰竭!
   - 轻微恐慌信号应输出负值(趋势恶化)
   - 这与黄金/避险资产的因子逻辑完全相反

## 当前挖掘方向

{direction_desc}

## 当前挖掘方法

{method_desc}

## 可用数据清单

{data_inventory}

## 已有因子 (禁止重复)

{existing_factors}

## 输出要求

请生成一个完整的 Python 类, 格式如下:

```python
import numpy as np
import pandas as pd

class <ClassName>Factor:
    \"\"\"因子名称 (挖掘方向/方法)

    逻辑: [一句话描述因子的经济学逻辑]
    数据: [使用的数据字段]
    输出: [信号含义]
    触发条件: [什么条件下信号非零, 预期Trigger Rate]
    \"\"\"

    def __init__(self, ...):
        self.name = '<factor_name>'

    def calculate_signal(self, data: pd.DataFrame) -> pd.Series:
        # 实现因子计算逻辑
        # 默认返回 0.0, 只在极端条件下输出非零值
        # 必须处理数据缺失的情况 (返回 0.0)
        signal = pd.Series(0.0, index=data.index)
        # ... 你的脉冲触发逻辑 ...
        signal.name = self.name
        return signal

    def __repr__(self):
        return f"{{self.__class__.__name__}}(...)"
```

注意:
- 类名使用 PascalCase, 以 Factor 结尾
- self.name 使用 snake_case
- calculate_signal 必须处理 data 中缺少所需列的情况 (返回全 0 Series)
- 信号默认为 0.0, 只在极端条件下输出 -1.0 或 +1.0 (脉冲型)
- 禁止使用未来数据 (no look-ahead bias)
- 禁止输出连续信号 (如 tanh 压缩后的连续值)
- 你的因子 Trigger Rate 必须在 5%-15% 之间, 否则将被废弃!
"""

USER_PROMPT_TEMPLATE = """请基于以下约束生成一个SPY卫星因子:

挖掘方向: {direction}
挖掘方法: {method}

要求:
1. 因子必须有清晰的经济学逻辑, 专门针对美股市场
2. 只使用上面数据清单中已有的字段
3. 信号方向: 看多美股为正, 看空美股为负
4. 关键: 对于panic_mean_reversion方向, 极端恐慌+衰竭 = 强烈看多(+1.0), 但必须等恐慌回落!
5. 代码必须完整可运行, 包含所有 import
6. 信号必须是脉冲型! 默认返回0.0, 只在极端事件瞬间触发! Trigger Rate 5%-15%!
7. 对于FOMC数据, 必须用.diff()捕捉边际变化, 禁止输出绝对值!

请直接输出 Python 代码, 不要解释。"""
